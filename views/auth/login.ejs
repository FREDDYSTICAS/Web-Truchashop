<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: linear-gradient(135deg, #1a2a6c, #b21f1f, #fdbb2d);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            position: relative;
        }

        /* Animación de burbujas en el fondo */
        .bubbles {
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: 0;
            overflow: hidden;
            top: 0;
            left: 0;
        }

        .bubble {
            position: absolute;
            bottom: -100px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            opacity: 0.5;
            animation: rise 10s infinite ease-in;
        }

        .bubble:nth-child(1) {
            width: 40px;
            height: 40px;
            left: 10%;
            animation-duration: 8s;
        }

        .bubble:nth-child(2) {
            width: 20px;
            height: 20px;
            left: 20%;
            animation-duration: 5s;
            animation-delay: 1s;
        }

        .bubble:nth-child(3) {
            width: 50px;
            height: 50px;
            left: 35%;
            animation-duration: 7s;
            animation-delay: 2s;
        }

        .bubble:nth-child(4) {
            width: 80px;
            height: 80px;
            left: 50%;
            animation-duration: 11s;
            animation-delay: 0s;
        }

        .bubble:nth-child(5) {
            width: 35px;
            height: 35px;
            left: 65%;
            animation-duration: 6s;
            animation-delay: 1s;
        }

        .bubble:nth-child(6) {
            width: 45px;
            height: 45px;
            left: 80%;
            animation-duration: 8s;
            animation-delay: 3s;
        }

        .bubble:nth-child(7) {
            width: 25px;
            height: 25px;
            left: 90%;
            animation-duration: 7s;
            animation-delay: 2s;
        }

        @keyframes rise {
            0% {
                bottom: -100px;
                transform: translateX(0);
                opacity: 0.5;
            }
            50% {
                transform: translateX(100px);
                opacity: 0.9;
            }
            100% {
                bottom: 100vh;
                transform: translateX(-100px);
                opacity: 0.2;
            }
        }

        /* Efecto de ondas en agua */
        .water-effect {
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 200px;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1440 320"><path fill="%230099ff" fill-opacity="0.15" d="M0,224L48,208C96,192,192,160,288,170.7C384,181,480,235,576,234.7C672,235,768,181,864,186.7C960,192,1056,256,1152,266.7C1248,277,1344,235,1392,213.3L1440,192L1440,320L1392,320C1344,320,1248,320,1152,320C1056,320,960,320,864,320C768,320,672,320,576,320C480,320,384,320,288,320C192,320,96,320,48,320L0,320Z"></path></svg>');
            background-size: cover;
            animation: wave 10s linear infinite;
        }

        @keyframes wave {
            0% {
                background-position-x: 0;
            }
            100% {
                background-position-x: 1440px;
            }
        }

        .login-container {
            width: 100%;
            max-width: 450px;
            z-index: 10;
            position: relative;
        }

        .login-card {
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
            padding: 40px 30px;
            text-align: center;
            transform: translateY(0);
            transition: transform 0.5s ease;
        }

        .login-card:hover {
            transform: translateY(-10px);
        }

        .login-header {
            margin-bottom: 30px;
        }

        .login-logo {
            width: 100px;
            margin-bottom: 15px;
            filter: drop-shadow(0 5px 10px rgba(0, 0, 0, 0.1));
            animation: float 3s ease-in-out infinite;
        }

        @keyframes float {
            0% {
                transform: translateY(0px);
            }
            50% {
                transform: translateY(-10px);
            }
            100% {
                transform: translateY(0px);
            }
        }

        .login-header h1 {
            color: #333;
            font-size: 24px;
            font-weight: 600;
            margin-bottom: 8px;
        }

        .login-header p {
            color: #666;
            font-size: 16px;
            margin-bottom: 20px;
        }

        .login-methods {
            margin-bottom: 30px;
        }

        .login-btn {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            padding: 15px 20px;
            background: #fff;
            border-radius: 50px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.08);
            text-decoration: none;
            color: #333;
            font-weight: 500;
            font-size: 16px;
            transition: all 0.3s ease;
            border: 1px solid #eaeaea;
            margin-bottom: 15px;
        }

        .login-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

        .google-btn {
            background: #fff;
            color: #757575;
        }

        .google-btn:hover {
            background: #f5f5f5;
        }

        .login-btn img {
            width: 24px;
            height: 24px;
            margin-right: 10px;
        }

        .login-footer {
            margin-top: 20px;
        }

        .login-footer p {
            font-size: 14px;
            color: #666;
        }

        .login-footer a {
            color: #1a73e8;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
        }

        .login-footer a:hover {
            color: #0d47a1;
            text-decoration: underline;
        }

        /* Efecto de brillos */
        .glow {
            position: absolute;
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            filter: blur(30px);
            animation: glow 8s linear infinite;
        }

        .glow:nth-child(1) {
            top: -50px;
            left: -50px;
            background: rgba(66, 133, 244, 0.2);
        }

        .glow:nth-child(2) {
            top: 30%;
            right: -70px;
            background: rgba(52, 168, 83, 0.2);
            animation-delay: 2s;
        }

        .glow:nth-child(3) {
            bottom: -80px;
            left: 20%;
            background: rgba(251, 188, 5, 0.2);
            animation-delay: 4s;
        }

        @keyframes glow {
            0% {
                transform: scale(1);
                opacity: 0.5;
            }
            50% {
                transform: scale(1.2);
                opacity: 0.8;
            }
            100% {
                transform: scale(1);
                opacity: 0.5;
            }
        }

        /* Responsive */
        @media screen and (max-width: 500px) {
            .login-container {
                width: 90%;
            }
            
            .login-card {
                padding: 30px 20px;
            }
            
            .login-header h1 {
                font-size: 20px;
            }
            
            .login-header p {
                font-size: 14px;
            }
            
            .login-btn {
                font-size: 14px;
                padding: 12px 15px;
            }
        }
    </style>
</head>
<body>
    <!-- Efecto de burbujas en el fondo -->
    <div class="bubbles">
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
        <div class="bubble"></div>
    </div>

    <!-- Efectos de brillo -->
    <div class="glow"></div>
    <div class="glow"></div>
    <div class="glow"></div>

    <!-- Efecto de ondas de agua -->
    <div class="water-effect"></div>

    <div class="login-container">
        <div class="login-card">
            <div class="login-header">
                <h1>Panel de Administrador</h1>
                <p>Accede para gestionar tu tienda de truchas</p>
            </div>
            
            <div class="login-methods">
                <a href="/auth/google" class="login-btn google-btn">
                    <img src="/img/google-icon.svg" alt="Google Logo">
                    <span>Iniciar sesión con Google</span>
                </a>
            </div>
            
            <div class="login-footer">
                <p><a href="/">Volver a la tienda</a></p>
            </div>
        </div>
    </div>

    <script src="/js/auth.js"></script>
    <script src="https://accounts.google.com/gsi/client" async defer></script>
</body>
</html>